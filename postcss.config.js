/**
 * PostCSS configuration for Tailwind CSS and autoprefixer.  Tailwind is
 * responsible for generating utility classes based on your project
 * configuration, while autoprefixer adds vendor prefixes to your CSS
 * where necessary to ensure broad browser support.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};